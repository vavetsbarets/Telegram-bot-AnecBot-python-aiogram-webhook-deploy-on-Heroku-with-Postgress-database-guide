# to run the bot on webhook, leave the line below as it is (change python file name, of course)
# if running on long pollng (not webhook), write: worker: python python_file.py
# (if you actually run or deploy the bot, delete all the lines starting with '#' in this file!!!!)
web: python bot_anecbot.py